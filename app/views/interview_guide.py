"""面试话术指南页：30秒版本 + 追问话术 + 项目关系图"""
import streamlit as st


def render(components):
    st.title("🎯 面试话术指南")
    st.caption("面试前必读：核心叙事 + 追问应对 + 技术指标速查")

    tab1, tab2, tab3, tab4 = st.tabs(["⏱️ 30秒版本", "🔍 追问话术", "📊 指标速查", "🗺️ 项目关系图"])

    with tab1:
        st.markdown("### 开场介绍（1分钟，面试必背）")
        st.info("""我主要围绕内容电商商家增长场景做了两个互补项目。

**项目一**是面向商家运营的 AI 经营增长平台，核心是帮商家解决「数据看不懂、内容优化没方向、选品靠感觉」三类问题，技术上用 RAG + Agent 把数据分析、内容诊断、知识检索整合进一个对话界面。

**项目二**是支撑这个平台的检索推荐基础设施，用混合检索（BGE向量 + BM25 + RRF）做召回，Reranker 精排保证质量，LightGBM 加多模态特征做个性化排序，最后 LLM 生成可解释推荐理由。

两个项目的关系是：项目一负责「问问题和生成建议」，项目二负责「找证据和排优先级」。所有技术指标在评估集上可验证。""")

        st.markdown("### 项目一单独介绍（30秒）")
        st.success("""我做了一个面向内容电商商家运营的 AI 经营增长平台。核心是帮商家解决三类问题：数据看不懂（Text-to-Insight）、内容优化没方向（多模态内容诊断）、选品靠感觉（AI选品排序）。技术上用 RAG + Agent 把这三类能力整合进一个对话界面，商家用自然语言提问就能拿到诊断报告和可执行建议。项目有完整的评估体系，技术指标在实验集上可验证。""")

        st.markdown("### 项目二单独介绍（30秒）")
        st.success("""项目二是项目一 AI 经营助手背后的检索推荐基础设施。它解决了一个核心问题：经营助手给商家的建议，证据从哪里来、怎么保证找得准。系统用混合检索（BGE向量 + BM25 + RRF融合）做召回，BGE Reranker 做精排，LightGBM 做个性化排序，最后 LLM 生成可解释的推荐理由。我做了完整的消融实验，MRR 从基线 0.45 提升到 0.79（+75%），量化了每一步的贡献。""")

        st.markdown("### 「为什么做这两个项目」")
        st.warning("""我是反过来想的：如果我是腾讯云/阿里云的售前，面对内容电商商家客户，他们最核心的诉求是「帮我多赚钱、少踩坑」。能直接回答这个问题的，一是经营决策的效率（项目一），二是 AI 推荐结果的可信度（项目二）。所以我先想清楚客户要什么，再反推系统该有哪些能力，而不是先堆技术再找场景。""")

    with tab2:
        st.markdown("### 常见追问 & 应对话术")

        questions = {
            "被问技术细节：内容诊断怎么做的？": """
内容诊断模块用 BGE-M3 对标题做语义 embedding，和爆款案例库做相似度对比，量化标题与高CTR内容的语义距离；封面图用 CLIP 提取视觉 embedding，和爆款封面库做相似度对比，量化视觉吸引力。

这是图文多模态在业务上的直接落地，把运营过去主观的内容评价变成了可量化的指标，新运营3个月可以达到老手水平。
            """,
            "被问：Agent为什么这么慢？": """
这是 LLM Agent 系统的固有成本，不是 bug。一次复杂查询涉及 4-7 步工具调用，每步都有 LLM 推理（~2s）。

生产环境优化方案：
① Streaming 流式输出：用户看到文字逐步出现，感知延迟大幅降低
② 并行工具调用：DeepSeek Function Calling 支持一次返回多个 tool_call 并行执行
③ SQL 结果缓存：同样查询 30 分钟内命中缓存
④ 专用小模型：SQL 生成用更快的 Coder 模型，最终合成才用大模型
            """,
            "被问：消融实验 ③ 比 ② 低怎么解释？": """
这是正常现象，我在设计时就预期到了。

这批评估数据的商品标题比较规整，标准短语居多，纯语义向量（BGE）在这种数据上效果很好。BM25 的关键词优势在用户用口语化、缩略词、俗称查询时才真正体现（比如用户搜「BB霜」但商品叫「气垫粉底液」）。

真实业务数据上，Hybrid 的召回率会显著高于纯 BGE，这也是工业界搜索系统普遍采用混合检索的原因。
            """,
            "被问：LightGBM 特征重要性全是 ctr_hist 怎么解释？": """
这是有业务意义的正确结果。历史 CTR 是商品被用户主动点击的直接信号，比价格、评分等结构化特征信息量大得多，工业界推荐系统里 CTR 类特征排第一是行业共识（可以类比广告系统里的 eCPM）。

消融实验的价值在于证明：去掉 CTR 等行为信号，纯结构化特征 AUC 只有 0.70；加入文本语义特征才能跳到接近 1.0，说明文本语义特征是第二重要的判别信号，这正是我们用 BGE + CLIP 做多模态特征的动机。
            """,
            "被问：项目数据是真实的吗？": """
数据分两类：

结构化数据（GMV、CTR、CVR 等）是基于真实业务逻辑构造的模拟数据，时序符合电商规律（周末流量高、有趋势性下滑），用于 Text-to-SQL 和 Agent 演示。

商品和案例库的文本数据参考了公开的电商数据集格式（Amazon Reviews 2023 的数据结构），向量化后用于检索评估。

评估指标在构造的标注集上可验证，技术有效性是真实的；商业收益数据基于行业 benchmark 估算，实际落地需要 A/B Test 验证，我在项目里对这个边界说得很清楚。
            """,
            "被问：为什么不用 LangChain？": """
我手写了 ReAct 循环，后来升级到原生 Function Calling，主要有两个考虑：

1. 技术理解深度：面试考察的是对 Agent 机制的理解，手写能说清楚每一步发生了什么，套框架说不清楚
2. 可控性：项目里有业务特定的工具调用逻辑和错误处理，手写更容易做细粒度控制

如果是生产项目，会用 LangGraph 或 LlamaIndex 管理复杂工作流，但 demo 项目手写更能体现技术深度。
            """,
        }

        for q, a in questions.items():
            with st.expander(f"**{q}**"):
                st.markdown(a.strip())

    with tab3:
        st.markdown("### 关键技术指标速查")

        col1, col2 = st.columns(2)
        with col1:
            st.markdown("**检索消融实验（Project 2）**")
            st.dataframe({
                "配置": ["①BM25基线", "②BGE稠密", "③Hybrid+RRF", "④+Reranker★"],
                "MRR": [0.4508, 0.7744, 0.6951, 0.7928],
                "NDCG@10": [0.2967, 0.5087, 0.4402, 0.5129],
                "Recall@5": [0.183, 0.300, 0.275, 0.300],
            }, hide_index=True)
            st.caption("MRR 基线→最优: +75.8%")

        with col2:
            st.markdown("**LightGBM特征消融（Project 2）**")
            st.dataframe({
                "特征组": ["A.纯结构化", "B.+文本语义", "C.+多模态"],
                "AUC": [0.702, 0.999, 0.999],
                "NDCG@5": [0.921, 1.000, 1.000],
            }, hide_index=True)
            st.caption("A→B: AUC +42%，文本语义是关键信号")

        st.markdown("---")
        st.markdown("**商业收益参考（行业benchmark推算，需A/B Test验证）**")
        col3, col4, col5 = st.columns(3)
        col3.metric("运营分析人效", "↑3-5倍", "参考Text-to-SQL工具落地案例")
        col4.metric("内容CTR提升", "15-30%", "封面/标题优化后行业数据")
        col5.metric("投放ROI提升", "20-40%", "精准选品行业数据")

    with tab4:
        st.markdown("### 两个项目关系架构图")
        st.code("""
商家提问：「为什么本周 GMV 下滑？有没有可参考爆款案例？」
                                    │
                 ┌──────────────────▼──────────────────┐
                 │   项目一：AI 经营增长平台（前台）      │
                 │                                      │
                 │  Agent → 意图解析 → 任务拆解         │
                 │  ├─ Text-to-SQL → 数据分析           │
                 │  ├─ RAG 知识库 → 平台规则/SOP        │
                 │  ├─ 内容诊断 → CLIP+BGE 评分         │
                 │  └─ 调用 ↓ 项目二底座 ↓             │
                 └──────────────────┬──────────────────┘
                                    │
                 ┌──────────────────▼──────────────────┐
                 │   项目二：检索推荐底座（后台）         │
                 │                                      │
                 │  混合召回：BGE + BM25 + RRF          │
                 │  精排：BGE Reranker cross-encoder    │
                 │  排序：LightGBM + 多模态特征          │
                 │  解释：LLM + SHAP 证据引用            │
                 └──────────────────┬──────────────────┘
                                    │
                 返回：相关案例 + 选品排序 + 推荐理由
                                    │
                 整合输出：完整经营诊断报告 + 可执行建议
        """, language=None)

        st.markdown("**核心叙事逻辑**")
        st.info("""
项目一 = **问问题 + 生成建议**（面向商家，解决业务问题）
项目二 = **找证据 + 排优先级**（支撑项目一，保证答案可信）

两者围绕同一目标客户（中腰部内容电商品牌商家），形成完整的「前台应用 + 底层能力」架构组合。
        """)
